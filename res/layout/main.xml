<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#000000">
    <ImageView
        android:id="@+id/ruby_image"
        android:layout_width="14sp"
        android:layout_height="14sp"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:background="#330022"
        android:src="@drawable/ruby"
        />
    <TextView
        android:id="@+id/ruby_count"
        android:layout_width="fill_parent"
        android:layout_height="14sp"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@id/ruby_image"
        android:gravity="right|top"
        android:background="#330022"
        android:textSize="12sp"
        android:text="0/1"
        />
    <ScrollView
        android:id="@+id/available_actions_scroll_view"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:padding="6dip"
        android:scrollbars="vertical">
        <LinearLayout
            android:id="@+id/available_actions"
            android:orientation="vertical"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:background="#660055"
            />
    </ScrollView>
    <ScrollView
        android:id="@+id/main_text_output_scroll_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ruby_count"
        android:layout_above="@id/available_actions_scroll_view"
        android:padding="6dip"
        android:scrollbars="vertical">
        <TextView
            android:id="@+id/main_text_output"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:textSize="18sp"
            android:text="Hello!"
            />
    </ScrollView>
</RelativeLayout>

